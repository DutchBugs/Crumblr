# Agent Integration track — local finding register

**This document is Dev-2-owned**, per
`review/CRUMBLR_DEV2_AGENT_INTEGRATION_INSTRUCTIONS_V3.md` §17. It uses
local workstream IDs (`AG-###`), never the project-wide reviewer `F-###`
series that lives in `review/FEEDBACK.md`. Do not create `feedback-dev2.*`
files — the one project-wide reviewer line stays `feedback.1.25.md` →
`feedback.2.0.md`.

These are self-identified gaps (found while building/reviewing this
track's own work against `review/THREAT_MODEL_AGENT_GATEWAY.md`), not
findings from an external reviewing agent. Escalate to the project-wide
reviewer only per instructions §19 (material safety ambiguity, a Phase-4
invariant needing to change, an authority-boundary dispute, an agent path
unexpectedly reaching execution, or a complete MVP readiness bundle).

---

## Open

| ID | Severity | Summary | Status | Closes when |
|---|---|---|---|---|
| AG-005 | MEDIUM (no exposure yet; escalates to HIGH if an ingestion path is proposed) | No evidence/news ingestion path exists — the SSRF mitigation in the threat model (§4.7) is a constraint on a not-yet-built system | OPEN — deferred to Step D by design | Step D, when an ingestion path is actually designed |
| F-064 (global, Dev-2-owned) | HIGH BEFORE REMOTE DEPLOYMENT / NOT A LOCAL-SHADOW BLOCKER | Reviewer finding, not self-identified — recorded here per instructions §17's "AG-### findings" ownership even though its number is in the global series (`feedback.1.26.md` §8; canonical register: `review/FEEDBACK.md`, Dev-1-owned). The HTTP transport is acceptable for local/in-process/controlled shadow use with the interim shared-secret mechanism, but is **not** authorized to be exposed publicly/remotely without TLS/mTLS or an equivalent deployment boundary, and must never gain admin routes | OPEN — not blocking current local/shadow work | Authenticated encrypted transport exists and is proven before any non-local agent operation |
| AG-012 | HIGH BEFORE REAL AGENT-DRIVEN SUBMISSION / NOT A SHADOW BLOCKER | Raised by Dev 1 (checked the actual `application/live_decision.py` flow, not a guess) while scoping the shared no-MT5 integration path (review 1.26 §7 item 3): `risk.policies.evaluate()`'s `PortfolioState.ledger` (`EquityLedger`) is stateful and per-process — `LiveDecisionOrchestrator` recovers it once via `risk.session.recover_session()` and holds/mutates it in memory across cycles, periodically persisting to `risk_session_states`, rather than reloading before every `evaluate()` call. If this Gateway's proposal path recovered and held its **own** independent copy of that same ledger in a separate process, an internal decision and an external-agent proposal could each pass the daily-loss/drawdown gate against a ledger that doesn't yet reflect the other's most recent trade — a lost-update race on one shared risk budget, invisible to either pipeline on its own. Two resolution paths, in order of preference: (1) route external-agent proposals into the *same running instance* that holds the canonical ledger, rather than a separate process reimplementing `recover_session()`; (2) if a separate process is architecturally necessary, re-recover the ledger fresh immediately before every `evaluate()` call instead of caching it in-process — not race-free without real DB locking, but avoids the worst staleness. **Not a blocker today**: `order_send` is unreachable regardless of which pipeline evaluates a proposal, so no real position can ever result from this gap — but it must be settled for real (most likely option 1) before `feedback.2.0` could ever treat agent-driven submission as real, not discovered live. `CapsuleStore.seal()` (content-derived `capsule_id`, `ON CONFLICT`-safe) and `evaluator.pretrade.evaluate()`'s currently-uncalibrated `intents_in_last_hour` check (`max_intents_per_hour: null` everywhere, F-024) are confirmed **not** to have the same issue today | OPEN — accepted as a shadow-mode-only limitation for the item 3 wiring, option 2 adopted (fresh `recover_session()` immediately before every Gateway-driven `evaluate()` call, never cached in-process); implemented in `agent_gateway/decision_path.py`, `tests/unit/test_agent_decision_path.py::TestAG012FreshSessionRecoveryEveryCall` | Option 1 (single shared risk-evaluation authority process) is designed and implemented before any agent-driven submission is treated as real |
| F-066 (global, Dev-2-owned portion) | HIGH BEFORE DIRECTIONAL EXTERNAL-AGENT SHADOW PROMOTION / `feedback.2.0` | Reviewer finding (`feedback.1.28.md`, canonical register `review/FEEDBACK.md`), tracked here per instructions §17 the same way F-064 is — the architectural resolution of AG-015 (see Closed below): Crumblr must be strategy-neutral. External context must carry trusted, strategy-neutral market/platform data, never a Core-computed strategy setup; the external agent owns its own strategy computation and reason-code vocabulary; the Gateway must accept strategy-local reason codes structurally/opaquely, never against a global whitelist; the external-agent Risk/Policy path must have no mandatory `baseline_v1`/`ict_v1`/Pivot-2-2 semantic dependency (item F, closed — see AG-013 in Closed below); a second toy/test agent with a deliberately different reason-code vocabulary must reach the same Core path with zero Core code changes (item 8 — the regression proof that a platform was built, not a single-strategy integration) | OPEN — item F (strategy-neutral Policy Gate) closed 2026-09-01, see AG-013 below. Items B/C (`AgentMarketContextV1`, opaque Gateway reason-code handling), G (fork-side coordination) and the full 9-condition closing list remain open | All nine `feedback.1.28.md` §10 conditions proven |
| AG-021 | LOW — not exploitable today, no execution reachable from this path regardless | `supervisor_review.py::evaluate_supervisor_review()` binds a `SupervisorReview` on `proposal_id`/`trade_intent_id`/`trade_intent_decision_hash`/expiry only — it never checks `risk_decision_id`/`policy_gate_decision_id`, even though `contracts.py::SupervisorReview` documents those two as completing "the audit chain from the external review back to the exact internal decisions it was reviewing." A future transport-backed Supervisor could echo back a review whose `risk_decision_id`/`policy_gate_decision_id` are stale (e.g. bound to a prior evaluation of the same intent) and it would still be accepted as fully bound. Both fields are optional on the contract ("a review may be requested before either has run"), so a strict check needs an explicit, not-yet-made decision about what "unset" means (deliberately not decided unilaterally this slice — self-review found the gap and it was tracked, not silently expanded into a design call) | OPEN — found via self-review while wiring Phase C (2026-09-03), documented in `supervisor_review.py`'s own module docstring so it is not silently assumed safe; `tests/unit/test_agent_decision_path.py::test_the_provider_is_passed_the_exact_risk_and_policy_decision_ids` proves what actually is enforced today | An explicit design decision for the unset-optional-field case, made deliberately (likely alongside real transport-backed Supervisor work), then enforced here |
| AG-022 | LOW — no real caller passes `external_supervisor` yet, so nothing is silently lost today | `agent_gateway/contracts.py::ExternalSupervisorReviewRecord` (Phase C's durable-record shape) has no durable persistence path: it cannot go through Core's shared event journal (`domain/events.py::EVENT_PAYLOAD_TYPES`) without `domain/` importing an agent_gateway-owned type, inverting this codebase's one-way dependency direction. `decision_path.py::evaluate_agent_trade_intent()` builds one and returns it on `AgentDecisionPathResult.external_supervisor_record`, but does not persist it — a caller that needs it kept (once one exists) must do so through its own mechanism | OPEN — found via self-review while wiring Phase C (2026-09-03); the original design (`recorder.record(...)`) would have raised `ValueError: ... is not a registered event payload` against the real `JournalRecorder` the first time any real caller supplied a provider — caught before shipping, not after | A cross-track decision on how agent_gateway-owned durable facts should be journaled (a generic "extension event" mechanism in Core, or routing through `AgentDecisionOutcomeStore` instead) — not decided unilaterally here |

## Closed

| ID | Severity | Summary | Status | Evidence |
|---|---|---|---|---|
| AG-013 | MEDIUM — no directional external-agent proposal could reach Supervisor APPROVE against real evidence; not a safety gap (fail-closed) | `agent_gateway/evidence.py::build_agent_context_evidence()` always sets `AgentContextEvidence.regime = Regime.UNKNOWN` by design (AG-006), and the old `evaluator.pretrade.SupervisorPolicy.veto_on_unknown_regime=True` meant `evaluator.pretrade.evaluate()` vetoed a directional external-agent intent unconditionally on `UNKNOWN_REGIME` — Risk `PASS` was reachable, Supervisor `APPROVE` was not | **CLOSED — resolved by `feedback.1.28.md`'s strategy-neutral architecture correction (F-066 item F), implemented 2026-09-01** | `agent_gateway/decision_path.py::_evaluate_platform_policy` replaces the `pretrade.evaluate()` call for the external-agent path entirely — checks only reconciliation/incident platform-safety health, never reads `features`/`Regime` at all. `SupervisorDecision.observed_regime` stays at its honest `Regime.UNKNOWN` default and gates nothing. `tests/unit/test_agent_decision_path.py::TestAG013Resolved` proves a directional proposal built from the real production evidence builder now reaches `APPROVE`; `TestStrategyNeutrality` proves an unrelated `strategy_id`/reason-code vocabulary reaches the identical outcome (F-066 item 8's regression proof, at this module's level) |
| AG-015 | HIGH — blocked `StaticAgentContextPayload` construction, an architectural question | Found while building feedback.1.27 §6 item B against the actual cloned `DutchBugs/crumblr-static-agent-host` fork: `crumblr_trader.py::CrumblrStaticTrader.evaluate()` validates `features.observation.reason_codes` against a closed, fork-specific vocabulary encoding Jari's "ICT Silver Bullet / Pivot 2.2" state machine, unconditionally, before branching on event_type — not a generic ICT vocabulary Crumblr's own `ict_v1` reason codes map onto honestly | **CLOSED — superseded by `feedback.1.28.md`'s architectural correction, tracked going forward as F-066 (Open, above)** | The reviewer's own conclusion: "the strategy computation is on the wrong side of the interface." All three mapping approaches this finding's own resolution notes considered are explicitly rejected by `feedback.1.28.md` section 2. See F-066 (Open table) for the ongoing, now project-wide tracking of the correction |
| AG-014 | HIGH | `evaluate_agent_trade_intent` (`agent_gateway/decision_path.py`) sealed a `RiskVerdict.HALT` or `SupervisorVerdict.HALT` capsule and returned without ever calling `kill_switch.trip()` — `risk.policies.evaluate()`/`evaluator.pretrade.evaluate()` only *name* a HALT in their verdict, they never trip the switch themselves (every caller's own responsibility; `application/live_decision.py`/`application/orchestration.py` both already honour this). A caller reusing the same `kill_switch` instance across evaluations — the exact usage the AG-012 mitigation assumes — would keep evaluating later intents as if nothing halting had happened: the next call's `policies.evaluate()` would not see `kill_switch.is_halted` and could return `PASS`/`APPROVE` even though a system-halting condition (`MAX_DRAWDOWN`, `DAILY_LOSS_LIMIT`, `RECONCILIATION_MISMATCH`/`UNKNOWN`, `WRONG_ACCOUNT`, `OVERNIGHT_EXPOSURE`, `LIVE_ACCOUNT_IN_PAPER_MODE`) had already been detected | **CLOSED — found by a `/code-review medium` self-review pass, fixed 2026-09-01, same day as opened, before first commit** | New `_trip()` helper in `decision_path.py`, called on `RiskVerdict.HALT` and on `SupervisorVerdict.HALT` (mirrors `application/live_decision.py`'s own `_trip`, including its `source="risk_engine"`-always convention on the recorded `SystemHalted` event, and its already-halted no-op guard). `tests/unit/test_agent_decision_path.py::TestHaltVerdictsTripTheKillSwitch` (3 tests: risk HALT trips it, supervisor HALT trips it, an already-halted switch is not re-tripped or double-recorded) |
| AG-016 | HIGH | `AgentDecisionOutcomeStore.append_event()` (`agent_gateway/stores.py`, `persistence/agent_gateway.py`) had no fail-closed conflict check at all — unlike every other "register/issue/claim" method this module's own docstring describes ("the same id with different content always raises, never silently overwrites"), a same-`(outcome_id, event_type)` re-append with genuinely different `reason_codes`/`detail` was silently discarded (`ON CONFLICT DO NOTHING` / in-memory `return`), never detected | **CLOSED — found via user-directed priority ("Agent Gateway event-conflict hardening"), fixed 2026-09-01. A regression was introduced and caught by the same-pass self-review before commit: the first fix compared `occurred_at_utc` too, which broke AG-008's resumed-but-unsettled retry path (`RECEIVED` is legitimately re-appended with a fresh wall-clock `now` on every resume) — every future retry would have permanently 409'd. Fixed to compare only `reason_codes`/`detail`, matching `_claim`'s own precedent of excluding claim timestamps from its fingerprint** | New `EventConflictError` (`agent_gateway/errors.py`), wired into `http.py`'s `_CONFLICT_TYPES` (409, not an unmapped 500). `tests/unit/test_agent_gateway_stores.py::TestAppendEventConflictDetection` (5 tests, including the `occurred_at_utc`-is-not-a-conflict regression), `tests/integration/test_agent_gateway_store.py::TestEventConflictIsDetectedUnderRealPostgres` (3 tests) proving the same against a real database, where the fix reads the already-committed row back on conflict rather than a stored fingerprint (no schema/migration change needed) |
| AG-017 | MEDIUM | `agent_gateway/static_agent_client.py::evaluate()`'s fail-closed contract ("raises a `StaticAgentTransportError` subclass only when the call itself failed") only wrapped `opener.open()` — a server that sends response headers/status promptly but then stalls mid-body (under load, or a slow/hostile endpoint) let a bare `TimeoutError`/`OSError` escape from the later `response.read()` call instead of the documented typed exception | **CLOSED — found by a `/code-review medium` self-review pass, fixed 2026-09-01, same day as opened, before first commit** | `response.read()`/`.status` now wrapped in its own `try`/`except TimeoutError, OSError` block, mapped to the same `StaticAgentTimeoutError`/`StaticAgentConnectionError` the connect-phase failure already used. `tests/unit/test_static_agent_client.py::TestRefusals::test_a_stall_mid_body_also_raises_the_typed_timeout` — a handler that sends headers/status immediately and only stalls the body write, reproducing the gap the original `test_a_slow_server_times_out` (which stalls before any bytes are sent) did not cover |
| AG-018 | HIGH | `agent_gateway/static_agent_translate.py::translate_no_trade_response()` originally took `decided_at_utc` as a caller-supplied parameter, expected to be the caller's own wall-clock "now" the same way every other `now`-shaped parameter in this codebase is. `NoTradeDecision.decision_fingerprint` (`contracts.py`) hashes `decided_at_utc`, so retrying the identical Static Agent response with a fresh wall-clock value produced the same deterministic `decision_id` but a *different* fingerprint — `AgentDecisionOutcomeStore._claim` treats that as a genuine conflict (`DecisionConflictError`), turning what the module's own docstring/tests advertised as a safe idempotent retry into a hard failure. The same class of bug AG-016 fixed for `append_event`'s `occurred_at_utc`, reintroduced here for `NoTradeDecision` via a different path | **CLOSED — found by a `/code-review medium` self-review pass, fixed 2026-09-01, same day as opened, before first commit** | `decided_at_utc` is no longer a parameter at all — derived deterministically from the response's own `decision_time_utc` (parsed, refuses if missing/malformed/timezone-naive). `tests/unit/test_static_agent_translate.py::TestTranslateRealForkResponse::test_repeated_translation_is_fully_retry_safe_including_the_fingerprint` (asserts full equality and matching `decision_fingerprint`, not merely `decision_id`), plus three new refusal tests for the parsing failure modes |
| AG-019 | MEDIUM | `agent_gateway/market_context.py::build_agent_market_context_v1()`'s bar-bounding used `snapshot.bars[-max_bars:]`, which for `max_bars=0` returns *all* bars, not zero — Python slicing treats `-0` as `0`, so `bars[-0:]` is `bars[0:]`. A caller passing `max_bars=0` (e.g. a future strategy-artifact manifest legitimately declaring "no bar history needed") would silently receive the full unbounded history instead, directly contradicting the "bounded window" requirement `feedback.1.28.md` section 3 names twice | **CLOSED — found by manual review, fixed 2026-09-02, same day as opened, before first commit.** The automated `/code-review` self-review pass could not run this session (hit a usage limit); this was caught by a deliberate manual re-read instead, in keeping with this track's established self-review discipline rather than skipping it when the tool was unavailable | Explicit `max_bars > 0` guard with a dedicated `else: ()` branch, plus a negative-`max_bars` refusal (`ValueError`) since the same slice expression silently wraps around for negative bounds too. `tests/unit/test_agent_market_context.py::TestMarket::test_max_bars_zero_yields_zero_bars_not_all_of_them`, `::test_a_negative_max_bars_is_refused` |
| AG-020 | MEDIUM | Adding structural bounds (`ReasonCodes` — count/length/safe-character) to `agent_gateway/contracts.py`'s `reason_codes` fields made `static_agent_translate.py::translate_no_trade_response()`'s final `NoTradeDecision(...)` construction able to raise a raw `pydantic.ValidationError` for a response that passed that module's own coarse `isinstance` checks but violated one of the new contract bounds (too many codes, a too-long one, a non-ASCII/control character) — contradicting that module's own documented contract ("raises `StaticAgentResponseRejectedError` ... before ever calling the Gateway") and every existing test asserting that exact exception type | **CLOSED — found by a `/code-review medium` self-review pass, fixed 2026-09-02, same day as opened, before first commit** | `NoTradeDecision(...)` construction wrapped in `try`/`except ValidationError`, re-raised as `StaticAgentResponseRejectedError`. `tests/unit/test_static_agent_translate.py::TestRefusals::test_refuses_reason_codes_that_violate_the_platform_contracts_own_bounds` (a non-ASCII reason code, which passes the module's own `isinstance` check but not the contract's charset bound) |
| AG-003 | HIGH | No Supervisor response-handling code exists — `UNKNOWN`-on-timeout/error is documented intent in the contract, not yet enforced behavior, and nothing calls the evaluation core or wires it into the decision path | **CLOSED — wired 2026-09-03, Phase C (`review/OWNER_WORK_ORDERS_DEMO_CANARY_2026-09-03.md`)** | `agent_gateway/decision_path.py::evaluate_agent_trade_intent` now calls the injected `ExternalSupervisorProvider` after the Policy Gate `APPROVE`s (never before — asking about an already-refused proposal is pointless), runs the result through `supervisor_review.py::evaluate_supervisor_review()`, and exposes both the outcome and a normalized `ExternalSupervisorReviewRecord` on `AgentDecisionPathResult`. New `agent_gateway/reference_supervisor.py::ReferenceSupervisor` is a real, deterministic, in-process implementation (Phase C explicitly permits this: "a deterministic reference Supervisor... is acceptable for the first DEMO canary") — checks only strategy-neutral, already-defined `TradeProposal` facts (non-empty `reason_codes`, a configurable `confidence` floor), never Risk/Policy state, never sizes/mutates/waives Risk/resets HALT. Backward compatible: both `proposal`/`external_supervisor` default to `None`, so every existing caller (PAPER_LITE included) is unaffected until it opts in. Remaining, honestly tracked, not blocking: no HTTP transport yet (in-process only — AG-... not opened, deliberately deferred per the module's own "narrow, real, proven" reasoning, same as before), `risk_decision_id`/`policy_gate_decision_id` binding not enforced (AG-021), no durable persistence path for the record yet (AG-022). `tests/unit/test_reference_supervisor.py` (12 tests), `tests/unit/test_agent_decision_path.py::TestExternalSupervisorWiring` (11 tests) — approve/veto/unknown paths, never-asked-when-refused (via a call-counting spy), full binding proof, and a `record_id`-collision regression (self-review found and fixed twice: first missed `reason_codes`, second pass included it) |
| AG-001 | HIGH | No `service_identity` authentication mechanism existed | **CLOSED — interim mechanism shipped 2026-08-28** | `agent_gateway/auth.py::hash_credential`/`verify_credential` (salted SHA-256, constant-time compare) + `AgentCredentialStore`. Not the final mTLS/SPIFFE-shaped mechanism `service_identity`'s own naming implies — a real, fail-closed, testable boundary in the meantime. `tests/unit/test_agent_gateway.py::TestIdentity` (6 tests) |
| AG-002 | HIGH | No idempotent-claim persistence for `proposal_fingerprint` existed | **CLOSED — shipped 2026-08-28** | `agent_decision_outcomes` table (`persistence/schema.py`), `PostgresAgentDecisionOutcomeStore`/`InMemoryAgentDecisionOutcomeStore` (`agent_gateway/stores.py`, `persistence/agent_gateway.py`) — same `INSERT ... ON CONFLICT DO NOTHING RETURNING` claim discipline `persistence/execution.py` proves for internal execution requests. Also extended to `NoTradeDecision` via a new `decision_fingerprint` computed field (`agent_gateway/contracts.py`), which did not exist at Step A. `tests/unit/test_agent_gateway.py::TestIdempotency` (4 tests), `tests/integration/test_agent_gateway_store.py::TestRestartSafety`/`TestConcurrentClaimIsAtomic` (4 tests) |
| AG-004 | HIGH | No assignment-scope server-side enforcement existed | **CLOSED — shipped 2026-08-28** | `AgentGateway._evaluate_proposal`/`_evaluate_no_trade` (`agent_gateway/gateway.py`) always look up `TradingAssignment` by `assignment_id` server-side and check ownership, validity window, risk-fraction band and rate limit — never trusts a proposal's own description of its scope (threat model §4.2). `tests/unit/test_agent_gateway.py::TestAssignmentScope` (6 tests) |
| AG-007 | HIGH | Proposal-rate-limit check was a classic check-then-act race: `count_claimed_since` was read in one transaction and `claim_trade_proposal` committed in a separate one, so two concurrent proposals for the same assignment could each observe a stale below-limit count and both get accepted, silently exceeding `max_proposals_per_hour` | **CLOSED — found by a self-review, fixed 2026-08-28** | `PostgresAgentDecisionOutcomeStore.lock_assignment()`/`.transaction()` (`persistence/agent_gateway.py`) — a Postgres transaction-scoped advisory lock (`pg_advisory_xact_lock`) serializes the whole claim→count→evaluate→settle sequence per `assignment_id`; `AgentGateway.submit_trade_proposal`/`submit_no_trade` hold it for the call's duration. Proven both ways: `tests/integration/test_agent_gateway_store.py::TestRateLimitIsAtomicUnderRealConcurrency` (10 real threads, real PostgreSQL, exactly `max_proposals_per_hour` accepted) was manually confirmed to *fail* (10/10 or 7/10 accepted) with the lock temporarily disabled, then re-confirmed green with it restored — the test is proven to catch the regression, not merely pass by luck |
| AG-008 | HIGH | `_replay` (idempotent-retry path) treated "no `REJECTED` event found" as proof of acceptance — a claim interrupted between the claim commit and the settle-event write (crash, exception) left a claimed-but-unsettled row that a retry would silently report `accepted=True` for, without ever running the actual authorization checks | **CLOSED — found by the same self-review, fixed 2026-08-28** | New `AgentDecisionOutcomeStore.settlement_for()` distinguishes "settled `ACCEPTED`/`REJECTED`" from "claimed but never settled." `AgentGateway.submit_trade_proposal`/`submit_no_trade` now resume evaluation with fresh inputs for the unsettled case rather than assuming any particular verdict — genuinely fail-closed, not merely refusing (a raise-based fix was considered and rejected: it would leave the outcome permanently stuck, since every retry would hit the same unsettled state). `agent_decision_events.event_id` also switched from random `uuid4()` to content-derived (mirrors `persistence/execution.py::event_id_for`), so a resumed attempt's re-appended `RECEIVED` event collapses to the existing row instead of duplicating. `tests/unit/test_agent_gateway.py::TestInterruptedClaimIsResumedNotAssumedAccepted` (2 tests) |
| AG-009 | MEDIUM | `TradingAssignment.required_evidence_fields` was defined in Step A but never checked — a proposal with `evidence_refs=()` was accepted even against an assignment that names required evidence fields | **CLOSED — found by the same self-review, fixed 2026-08-28** | `AgentGateway._evaluate_proposal` rejects with `AgentRejectionReason.MISSING_REQUIRED_EVIDENCE` when `assignment.required_evidence_fields` is non-empty and `proposal.evidence_refs` is empty. Deliberately conservative, documented as such: proves *some* evidence was cited, does not verify the cited evidence covers each named field — that needs evidence-content inspection, out of scope until AG-005's ingestion path exists. `tests/unit/test_agent_gateway.py::TestRequiredEvidence` (3 tests) |
| AG-006 | MEDIUM — blocked `TradeProposal → TradeIntent` mapping | The internal `TradeIntent` contract requires a non-optional `feature_snapshot_id: UUID`; an externally-originated proposal had no computed feature snapshot. **Resolved by `feedback.1.26.md` §5**, not by the Dev-1 `compute_features()` extraction both tracks were pursuing — the reviewer explicitly rejected that path ("AG-006 is not a reason to first invent one universal `compute_features()`"). Instead: a new, deliberately narrow, honestly-named platform-owned evidence shape (`feature_set_version = "agent_context_v1"`), reusing the existing generic `FeatureEvidence`/`FeatureSnapshotStore` persistence layer unmodified. Crumblr creates and durably records this evidence *before* issuing the `DecisionContextBundle` that references it; `AgentGateway.issue_context_bundle` refuses a bundle whose `feature_snapshot_id` does not resolve to a real stored snapshot; `bundle.content_hash` includes the reference so it cannot be swapped post hoc | **CLOSED — implemented and tested 2026-09-01** | New `agent_gateway/evidence.py::AgentContextEvidence`/`build_agent_context_evidence` (structurally satisfies `trading_agent.base.FeatureEvidence`, verified via `isinstance()`, `regime` always `Regime.UNKNOWN` — honest, not guessed). `DecisionContextBundle.feature_snapshot_id` (new required field, included in `content_hash`). New `AgentGateway.publish_context()` — the "platform context publication" entry point (records evidence, then issues the bundle referencing it); `issue_context_bundle` independently fails closed on an unresolvable snapshot (`UnknownFeatureSnapshotError`). New `FeatureEvidenceStore` Protocol + `InMemoryFeatureEvidenceStore` (`agent_gateway/stores.py`); the real `persistence/features.py::FeatureSnapshotStore` satisfies it unmodified — no schema/migration change needed. Tests: `tests/unit/test_agent_gateway_contracts.py` (+1, content_hash reacts to `feature_snapshot_id`), `tests/unit/test_agent_gateway.py::TestFeatureEvidence` (4), `tests/integration/test_agent_gateway_store.py::TestFeatureEvidenceAgainstRealPostgres` (3, including a restart-safety case against the real `feature_snapshots` table) |
| AG-010 | HIGH | `_build_trade_intent` originally sourced `TradeIntent.model_version` from a live `AgentIdentity` lookup at mapping time. Identity is a mutable, append-only-latest-wins snapshot (`register_identity` can change it any time), so a replayed retry after a re-registration between the original acceptance and the retry would reconstruct a *different* `TradeIntent` (different `model_version`, different `decision_hash`) than the original — directly contradicting the documented "byte-identical reconstruction" determinism guarantee | **CLOSED — found by a self-review, fixed 2026-09-01, same day as opened** | `_build_trade_intent` no longer takes an `AgentIdentity` parameter at all; `model_version` is always `None`. Real per-agent-model provenance would need its own immutable, content-addressed home (e.g. a field on `TradeProposal` itself), not a live side-channel lookup — not built now, not needed for the mapping to be correct. `tests/unit/test_agent_gateway.py::TestTradeIntentMapping::test_model_version_is_never_sourced_from_a_live_identity_lookup` — re-registers the agent with a different `model_version` between two submissions of the identical proposal and asserts the reconstructed `TradeIntent` is unchanged |
| AG-011 | MEDIUM | `TradingAssignment.strategy_artifact_hash` was an unconstrained `str`, but `_build_trade_intent` copies it directly into `TradeIntent.strategy_version`, a `VersionTag` (1-128 chars). An assignment registered with an empty or over-long hash would register successfully and then crash the *first* proposal accepted against it with an uncaught `pydantic.ValidationError` — in `agent_gateway/http.py` this exception type isn't in the error map, so it would have surfaced as an unhandled `500` at the wire boundary, repeating on every future submission against that same (immutable) assignment | **CLOSED — found by the same self-review, fixed 2026-09-01** | `agent_gateway/contracts.py::TradingAssignment.strategy_artifact_hash` retyped from `str` to `VersionTag`, closing the gap at registration time — a malformed value now fails closed the moment an assignment is registered, never deep inside proposal acceptance. `tests/unit/test_agent_gateway_contracts.py::TestTradingAssignment::test_an_empty_strategy_artifact_hash_is_rejected` |

---

## Notes

- Full threat rationale for each item above lives in
  `review/THREAT_MODEL_AGENT_GATEWAY.md` §7, which this table summarizes;
  that document is the source of truth for *why*, this table is for
  *status*.
- A finding here moves to Closed only with evidence (a commit, a file, a
  test), same discipline as the project-wide register in
  `review/FEEDBACK.md`.
- **None of AG-003/AG-005/F-064 are exploitable today.** An HTTP transport
  now exists (`agent_gateway/http.py`, 2026-08-31) but is not deployed
  anywhere (no `uvicorn` invocation, no port, no TLS) — see F-064's own
  row. Nothing outside `src/crumblr/agent_gateway/` and
  `src/crumblr/persistence/agent_gateway.py` imports this track's code —
  verified by grep, most recently 2026-09-01.
